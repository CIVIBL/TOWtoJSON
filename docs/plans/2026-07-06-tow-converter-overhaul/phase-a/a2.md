# A2: Rewire index.html to import modules and delete inline copies

**Avoid:** Don't leave any duplicate function bodies behind — the whole point is one implementation. Grep for each extracted function name after editing; each must appear only as an import.

**Avoid:** Don't switch to `file://` testing — ES module imports and `fetch('src/data/...')` both require http. Use `npm run serve` (port 8080).

## Steps

### Step 1: Replace the inline implementation with imports

At the top of the `<script type="module">` block add:

```js
import { parseWithFactionData } from './src/js/parse.js';
import { buildMagicItemIndex } from './src/js/items.js';
import { detectFactionFromText, detectArmyOfInfamyFromText, getArmyCompositions } from './src/js/detect.js';
import { generateOWBJson } from './src/js/generate.js';
```

Delete the moved function bodies (everything A1 extracted). Keep in index.html: `loadFactionData`, `loadMagicItems`, `factionDataCache`, `magicItemsData`, `debounce`, `downloadJson`, `showFeedback`, and all DOM/event code. Update the call site `buildSmartMagicItemIndex(...)` → `buildMagicItemIndex(...)`.

### Step 2: Delete the dead inline layer

Remove entirely (defined but never called — verified): `normalizeString`, `buildUnitIndex`, `buildMagicItemsIndex`, `fuzzyScore`, `findUnit`, `findInArrayFuzzy`, `classifyToken`, `matchUnit` (index.html lines ~1040-1200).

### Step 3: Verify in browser

Start the server via preview tools (`.claude/launch.json`: `{"name":"serve","runtimeExecutable":"python","runtimeArgs":["-m","http.server","8080"],"port":8080}`). Paste the Skaven test list from CLAUDE.md, convert, confirm: faction detected as Skaven, 10 units reported, JSON downloads. Check console for errors.

### Step 4: Verify no duplication

`grep -c "function parseWithFactionData" index.html` → 0. Same for `extractOptions`, `buildUnit`, `detectFactionFromText`.

Commit: `refactor: index.html imports src/js modules, dead inline layer removed`
