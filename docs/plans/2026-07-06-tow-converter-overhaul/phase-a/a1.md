# A1: Extract inline implementation into src/js/ ES modules

**Avoid:** Do NOT extract from `src/parser/`, `src/matcher/`, or `src/generator/` — those are the OLD dead pipeline (they matched only 5/10 Skaven units). The live implementation is the inline `<script type="module">` in `index.html` (lines ~182-1362). Extract that, verbatim, no behavior changes in this task — Phase B changes behavior against tests that don't exist yet.

**Avoid:** No `fetch`, `document`, or `window` references inside the modules — tests run them in Node. Data loaders and DOM code stay in index.html.

## Steps

### Step 1: Create src/js/parse.js

Move these functions from index.html, unchanged:
`buildUnitNameIndex`, `generateNameVariations`, `parseSectionHeaders`, `getCategoryAtPosition`, `extractHeaderPoints`, `findUnitAnchors`, `buildDetachmentIndex`, `extractDetachments`, `parseWithFactionData`.

Add at top: `import { matchUnitName, extractOptions } from './match.js';`
Export all functions. `parseWithFactionData(text, factionData, magicItemIndex)` keeps its signature.

### Step 2: Create src/js/match.js

Move: `matchUnitName`, `nameMatchesInContext`, `extractOptions`. Export all three.

### Step 3: Create src/js/items.js

Move `buildSmartMagicItemIndex` and export it as `buildMagicItemIndex(magicItemsData, factionSlug)` (rename; keep the currently-unused `factionSlug` param — B2 will use it).

### Step 4: Create src/js/detect.js

Move: `FACTION_CONFIG`, `normalizeFactionName`, `detectFactionFromUnits`, `getFactionSlug`, `ARMY_OF_INFAMY_DETECTION`, `detectArmyOfInfamy`, `getArmyCompositions`, `detectFactionFromText`, `detectArmyOfInfamyFromText`. Export the functions and both config consts.

### Step 5: Create src/js/generate.js

Move: `generateIdSuffix`, `deepClone`, `buildUnit`, `generateOWBJson`. Export `buildUnit` and `generateOWBJson`.

### Step 6: Verify imports

```
node -e "await import('./src/js/parse.js'); await import('./src/js/match.js'); await import('./src/js/items.js'); await import('./src/js/detect.js'); await import('./src/js/generate.js'); console.log('ok')"
```

Do NOT edit index.html in this task (A2 owns it). Commit: `feat: extract live converter implementation into ES modules`
