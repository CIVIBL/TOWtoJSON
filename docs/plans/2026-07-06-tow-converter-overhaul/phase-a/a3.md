# A3: Replace demo test scripts with assertions; delete dead src modules

**Avoid:** Don't assert option-level matching correctness yet — current matching is substring-based and wrong in known ways; Phase B fixes it against B-phase tests. Here, assert only parse/detect/generate facts that are already true and must never regress.

**Avoid:** Don't import from `src/parser|matcher|generator` — you are deleting them in this task.

## Steps

### Step 1: Create test/fixtures/skaven-bcp.txt

Copy the exact Skaven BCP list from CLAUDE.md "Test Data" (from `Skaven` through the final `210 - Hell Pit Abomination`).

### Step 2: Create test/parse.test.js

Use `node:test` + `node:assert/strict`. Load data with `fs.readFileSync` + `JSON.parse` (paths relative to repo root via `new URL('../src/data/owb/skaven.json', import.meta.url)`).

Assert:
- `findUnitAnchors(fixture)` returns 10 anchors; sum of `points` === 2197
- Anchor model counts: Clanrats anchors have modelCount 31 and 30; Giant Rats 14
- `parseWithFactionData(fixture, skavenData, itemIndex).units.length === 10`
- Every unit has `success === true` (all 10 Skaven names exist in skaven.json — if one fails, the name-matching bug is in scope for Phase B; mark that single assertion `todo` rather than deleting it)

### Step 3: Create test/detect.test.js

Assert:
- `detectFactionFromText(fixture) === 'skaven'`
- `detectFactionFromText('Wood Elves\n\n100 - Glade Guard')` === 'wood-elf-realms'
- `detectFactionFromText('completely unrelated text')` === null

### Step 4: Create test/generate.test.js

Run the full pipeline on the fixture and assert:
- Output has keys: name, game='the-old-world', army='skaven', characters/core/special/rare arrays, armyComposition
- Unit ids match `/^[a-z-]+\.[a-z]{8}$/`
- Grey Seer entry has an active Level 4 wizard nested option and activeLore 'elementalism'

### Step 5: Update package.json, delete dead code

Scripts: `"test": "node --test test/"`. Delete `src/parser/`, `src/matcher/`, `src/generator/`, `test/testParser.js`, `test/testMatcher.js`, `test/testGenerator.js`, `test/output-skaven.json`.

### Step 6: Run and commit

`npm test` green. Commit: `test: assertion suite against live modules; remove dead pipeline`
