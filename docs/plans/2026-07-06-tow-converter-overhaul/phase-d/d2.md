# D2: Render report in feedback panel

**Avoid:** Don't block the download on warnings — users with partial lists still want the file. But never show a plain success state when anything was dropped: warnings must be visually louder than the success line.

## Steps

### Step 1: Update the convert handler in index.html

After generating, call `buildReport(...)` and render:

- Success line: `Matched 9 of 10 entries — Skaven, Clan Pestilens, 2197 pts` (use `aged-crimson`/`aged-gold` accents when matched < total).
- Dropped units section (crimson): unit name + points, with hint "check spelling or report an issue on GitHub".
- Unknown tokens per unit (gold): `Grey Seer: "Made Up Thing" not recognized — it will be missing from the export`.
- Points check line: parsed vs computed total; per-unit mismatches collapsed under a `<details>` element.
- Keep download automatic; place the report above the fold.

### Step 2: Manual verification

`npm run serve`; convert (a) the clean fixture → all-green report; (b) fixture with a misspelled unit and a fake item → dropped unit and unknown token clearly shown, file still downloads.

Commit: `feat: honest feedback panel with dropped-unit and token warnings`
