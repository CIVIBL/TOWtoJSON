# E3: Enable New Recruit UI toggle

**Avoid:** Don't gate the whole convert flow on NR data existing for every faction — only skaven and wood-elf-realms ship in E1. If the selected faction has no `src/data/nr/<faction>.json`, keep the NR button enabled but show "New Recruit export not yet available for <faction>" in the feedback panel and don't download.

## Steps

### Step 1: Update index.html

- Remove `disabled` and "(soon)" from `btn-nr`; implement its click handler symmetric to `btn-owb` (the active-state class swap currently only exists on the OWB button).
- Convert handler: when `selectedFormat === 'nr'`, lazily fetch `src/data/nr/game-system.json` + `src/data/nr/<faction>.json` (404 → the graceful message above), call `generateNRJson`, merge its `warnings` into the D-phase report rendering, download as `<faction>_<pts>pts.nr.json`.

### Step 2: Manual verification

`npm run serve`:
1. Skaven fixture + NR format → file downloads, warnings shown, imports into newrecruit.eu.
2. Empire list + NR format → graceful "not yet available" message, no download, no console error.
3. OWB path unchanged.

Commit: `feat: New Recruit export enabled in UI`
